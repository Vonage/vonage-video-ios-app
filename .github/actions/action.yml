name: install-tuist
description: Install Tuist
runs:
  using: composite
  steps:
    - name: Install Tuist
      run: |
        if ! command -v tuist >/dev/null 2>&1; then
          brew install tuist
        else
          echo "Tuist already installed: $(tuist version)"
        fi
      shell: bash